# Database Configuration
DATABASE_URL=jdbc:mysql://localhost:3306/expenses_db
DATABASE_USERNAME=root
DATABASE_PASSWORD=your_password_here

# JWT Configuration
# IMPORTANT: Use a strong secret key in production (minimum 64 characters)
JWT_SECRET=your_super_secret_key_that_must_be_at_least_64_characters_long_for_security_purposes

# Email Configuration (for OTP)
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password_here

# CORS Configuration
# Add your frontend URLs here (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4200

# Server Configuration (optional)
SERVER_PORT=8080

# Profile (dev, prod)
SPRING_PROFILES_ACTIVE=dev

